# Release Notes

## v1.0 — Initial Stable Release

### What's new
- Server startup and health check endpoints
- Basic configuration via config.yaml
- Structured logging

### Breaking changes
- None (first stable release)

### Known issues
- Metrics endpoint is stubbed (returns 0 always)

---

## How to upgrade

```bash
git fetch origin --tags
git checkout v1.0
```
